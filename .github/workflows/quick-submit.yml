name: Quick Submit

on:
  pull_request:
    types: [opened, reopened]
    branches: [agentbeats-e2e-quick-submit-f6b295ceda]

jobs:
  run-scenario:
    if: startsWith(github.head_ref, 'quick-submit-')
    uses: RDI-Foundation/agentbeats-leaderboard-template/.github/workflows/quick-submit-runner.yml@v2
    with:
      backend_url: "https://sizes-ballet-crew-trunk.trycloudflare.com"
    permissions:
      id-token: write
      contents: write
      packages: read
