name: Quick Submit

on:
  pull_request:
    types: [opened, reopened]
    branches: [agentbeats-e2e-quick-submit-6ea0f86659]

jobs:
  run-scenario:
    if: startsWith(github.head_ref, 'quick-submit-')
    uses: RDI-Foundation/agentbeats-leaderboard-template/.github/workflows/quick-submit-runner.yml@v2-matrix
    with:
      backend_url: "https://mattress-lane-tool-cordless.trycloudflare.com"
    permissions:
      id-token: write
      contents: write
      packages: read
